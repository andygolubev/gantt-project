name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build-and-push:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build --file Dockerfile.nginx --tag gantt:latest .
    - name: Login to ECR
      id: ecr
      uses: jwalton/gh-ecr-login@v3
      with:
        access-key-id: "AKIAQ3EGVKIYM336KWN6"
        secret-access-key: "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"
        region: us-east-1
    - name: Tag the Docker image for ECR
      run: docker tag gantt:latest ${{ steps.ecr.outputs.account }}.dkr.ecr.us-east-1.amazonaws.com/gantt:latest
    - name: Push to ECR
      run: |
        docker push ${{ steps.ecr.outputs.account }}.dkr.ecr.us-east-1.amazonaws.com/gantt:latest
